#include <string>
#include <vector>
#include <cmath>

using namespace std;

long long solution(long long n) {
    long long answer = -1;
    int a = sqrt(n);
    if (!(sqrt(n) - a)) return pow(sqrt(n) + 1, 2);

    return answer;
}